#!/bin/bash
# Lyra shell - global output mode flags

# JSON output mode (set via --json flag)
export LYRA_OUTPUT_JSON="${LYRA_OUTPUT_JSON:-false}"

# Quiet mode (set via --quiet or -q flag)
export LYRA_QUIET="${LYRA_QUIET:-false}"


