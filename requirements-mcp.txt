# Lyra MCP Server Dependencies (WSL Host Execution)
# Lightweight subset for running MCP server on WSL host.
# Excludes heavy ML dependencies (PyTorch, PaddleOCR, ONNX, etc.)
# which run in containers via proxy.

# === Core Dependencies ===
pydantic>=2.5.0
pydantic-settings>=2.1.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
aiohttp>=3.9.0
aiofiles>=23.2.1
jinja2>=3.1.0

# === MCP Server ===
mcp>=1.0.0

# === HTTP Client (Browser Impersonation) ===
curl-cffi>=0.6.0

# === Browser Automation ===
playwright>=1.40.0

# === Content Extraction ===
trafilatura>=1.6.0
beautifulsoup4>=4.12.0
readability-lxml>=0.8.1

# === Japanese NLP (for BM25 tokenization) ===
sudachipy>=0.6.7
sudachidict-core>=20231110

# === Text Processing & Search ===
rank-bm25>=0.2.2
tldextract>=5.1.0

# === Graph ===
networkx>=3.2.0

# === Database ===
aiosqlite>=0.19.0

# === HTTP Client (for proxy calls) ===
httpx>=0.26.0

# === Archive & WARC ===
warcio>=1.7.4

# === Logging ===
structlog>=23.2.0

# === Similarity (for deduplication) ===
datasketch>=1.6.0


