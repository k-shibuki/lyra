{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "lancet:mcp:notify_user",
  "title": "notify_user Response",
  "description": "Response schema for notify_user tool",
  "type": "object",
  "properties": {
    "ok": {
      "type": "boolean"
    },
    "event": {
      "type": "string",
      "enum": ["auth_required", "task_progress", "task_complete", "error", "info"]
    },
    "notified": {
      "type": "boolean"
    }
  },
  "required": ["ok", "event", "notified"],
  "additionalProperties": false
}

