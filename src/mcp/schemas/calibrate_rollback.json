{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "lancet:mcp:calibrate_rollback",
  "title": "calibrate_rollback Response",
  "description": "Response schema for calibrate_rollback tool (destructive operation)",
  "type": "object",
  "properties": {
    "ok": {
      "type": "boolean"
    },
    "source": {
      "type": "string"
    },
    "rolled_back_to": {
      "type": "integer"
    },
    "previous_version": {
      "type": "integer"
    },
    "reason": {
      "type": "string"
    },
    "brier_after": {
      "type": ["number", "null"]
    },
    "method": {
      "type": "string"
    }
  },
  "required": ["ok", "source", "rolled_back_to", "previous_version"],
  "additionalProperties": false
}

