-- Claim origins (provenance: which fragment/page a claim was extracted from)
SELECT *
FROM v_claim_origins
{% if task_id %}
WHERE task_id = '{{ task_id }}'
{% endif %}
ORDER BY claim_id, origin_created_at
