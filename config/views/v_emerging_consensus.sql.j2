-- Claims with increasing recent supporting evidence
SELECT *
FROM v_emerging_consensus
{% if task_id %}
WHERE task_id = '{{ task_id }}'
{% endif %}
ORDER BY support_trend DESC, recent_support_count DESC


