-- Claim evidence aggregation (supports/refutes/neutral counts)
SELECT *
FROM v_claim_evidence_summary
{% if task_id %}
WHERE task_id = '{{ task_id }}'
{% endif %}
ORDER BY evidence_count DESC, refute_count DESC


