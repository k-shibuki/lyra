%%{init: {'theme': 'neutral'}}%%
flowchart LR
    subgraph Pages
        P1["Page A"]
        P2["Page B"]
    end

    subgraph Fragments
        F1["Fragment<br/>(from Page A)"]
        F2["Fragment<br/>(from Page B)"]
    end

    C["Claim"]

    %% Fragment belongs to Page (implicit via page_id FK)
    P1 -.-> F1
    P2 -.-> F2

    %% ORIGIN: provenance tracking
    F1 ==>|ORIGIN| C

    %% NLI: cross-source verification
    F2 -->|supports / refutes| C

    %% CITES: citation between pages
    P1 -->|CITES| P2

    %% EVIDENCE_SOURCE: derived (not persisted)
    C -.->|EVIDENCE_SOURCE| P1

