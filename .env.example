# Lancet Environment Configuration
# Copy this file to .env and modify as needed.
# .env is gitignored and will not be committed.
#
# Environment variables override settings.yaml values.
# Format: LANCET_<SECTION>__<KEY>=value (double underscore for nesting)

# === Browser Settings ===
# Chrome remote debugging host (for WSL2/Podman → Windows Chrome connection)
# Default: localhost
# For WSL2: Use the WSL2 gateway IP (run: ip route | grep default | awk '{print $3}')
# LANCET_BROWSER__CHROME_HOST=xxx.xx.xxx.x

# Chrome remote debugging port
# LANCET_BROWSER__CHROME_PORT=9222

# === LLM Settings ===
# Ollama host URL
# LANCET_LLM__OLLAMA_HOST=http://localhost:11434

# === Tor Settings ===
# Tor SOCKS proxy host
# LANCET_TOR__SOCKS_HOST=127.0.0.1

# === General Settings ===
# Log level (DEBUG, INFO, WARNING, ERROR)
# LANCET_GENERAL__LOG_LEVEL=INFO
