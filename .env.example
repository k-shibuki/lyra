# Lancet Environment Configuration
# Copy this file to .env and modify as needed.
# .env is gitignored and will not be committed.
#
# Environment variables override settings.yaml values.
# Format: LANCET_<SECTION>__<KEY>=value (double underscore for nesting)

# =============================================================================
# USER-SPECIFIC SETTINGS
# =============================================================================

# Chrome CDP connection (WSL2 -> Windows Chrome)
# LANCET_BROWSER__CHROME_PORT=19222

# =============================================================================
# CONTAINER NETWORKING (Required for Podman)
# =============================================================================

# Ollama LLM server (container name)
# LANCET_LLM__OLLAMA_HOST=http://ollama:11434

# Tor SOCKS proxy (container name)
# LANCET_TOR__SOCKS_HOST=tor
# LANCET_TOR__SOCKS_PORT=9050
# LANCET_TOR__CONTROL_PORT=9051

# ML Server (internal network - lancet-internal)
# LANCET_ML__SERVER_URL=http://lancet-ml:8100

# =============================================================================
# ML SERVER SETTINGS (for lancet-ml container)
# =============================================================================

# GPU usage (set to false for CPU-only)
# LANCET_ML__USE_GPU=true

# Model names (defaults shown, uncomment to override)
# LANCET_ML__EMBEDDING_MODEL=BAAI/bge-m3
# LANCET_ML__RERANKER_MODEL=BAAI/bge-reranker-v2-m3
# LANCET_ML__NLI_FAST_MODEL=cross-encoder/nli-deberta-v3-xsmall
# LANCET_ML__NLI_SLOW_MODEL=cross-encoder/nli-deberta-v3-small

# =============================================================================
# SCRIPT SETTINGS (for scripts/*.sh)
# =============================================================================

# Socat port for WSL2 -> Windows Chrome forwarding (used by chrome.sh)
# LANCET_SCRIPT__SOCAT_PORT=19222

# Container name (used by all scripts)
# LANCET_SCRIPT__CONTAINER_NAME=lancet

# Container startup timeout in seconds
# LANCET_SCRIPT__CONTAINER_TIMEOUT=30

# Test completion detection threshold in seconds
# LANCET_SCRIPT__COMPLETION_THRESHOLD=5

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR
# LANCET_GENERAL__LOG_LEVEL=INFO
